unbind r
bind r source-file ~/.tmux.conf
# Change the prefix from default <C-b> to <C-s>

set -g prefix C-s


# Use vim keybindings to navigate.

setw -g mode-keys vi
bind-keys h select-pane -L
bind-keys j select-pane -D
bind-keys k select-pane -U
bind-keys l select-pane -R

