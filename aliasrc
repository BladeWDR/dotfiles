alias ssha='eval $(ssh-agent) && ssh-add'
alias dcfr='docker-compose up -d --force-recreate'
alias dcu='docker-compose up -d'
alias config='/usr/bin/git --git-dir=$HOME/.cfg/.git --work-tree=$HOME'
#alias ls='ls -l'
alias ..="cd .."
alias whatup="sudo systemctl status"
#alias cat="batcat"
alias config='/usr/bin/git --git-dir=$HOME/.cfg/ --work-tree=$HOME'
alias pyans='source /home/scott/ansible/bin/activate'
alias gfetch='git fetch && git status'
unalias dps 2>/dev/null
unalias dpsp 2>/dev/null
dps() { docker ps "$@" --format "{{.Names}}\t{{.Status}}\t{{.Ports}}\t{{.Image}}\t{{.ID}}\t{{.State}}" | dps-format; }
alias dpsp='dps -a'
alias ffgif='ffmpeg -i input.mp4 -vf "fps=10,scale=320:-1:flags=lanczos,split[s0][s1];[s0]palettegen[p];[s1][p]paletteuse" -loop 0 output.gif'
alias ssh='TERM=xterm-256color ssh'
alias lg='lazygit'
alias lzd='lazydocker'
alias v='nvim'
alias ls='eza -l'
alias cz='chezmoi'
alias cga='chezmoi git pull && cz apply'
alias hl='rg --passthru' # Uses ripgrep to highlight search terms. Example use: cat file | hl searchterm
alias bwu='bitwarden-unlock'
alias bws='bitwarden-search'
alias bwp='bitwarden-get-password'
